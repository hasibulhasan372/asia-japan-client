

const SectionTitle = ({title}) => {
    return (
        <div className="">
            <h2 className="text-[#011E40] text-4xl sm:text-5xl font-semibold text-center">{title}</h2>
        </div>
    );
};

export default SectionTitle;